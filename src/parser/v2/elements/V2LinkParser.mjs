// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Slugify from "../utils/Slugify.mjs";
import * as Priority from "../parser/Priority.mjs";
import * as TokenStream from "../lexer/TokenStream.mjs";
import * as V2ElementParser from "./V2ElementParser.mjs";

function canParse(stream) {
  let tok = TokenStream.peek(stream);
  let match = tok.kind;
  if (typeof match !== "object" && match === "LAngle") {
    return TokenStream.findOnCurrentRow(stream, t => {
      let match = t.kind;
      if (typeof match !== "object") {
        return match === "RAngle";
      } else {
        return false;
      }
    }) !== undefined;
  } else {
    return false;
  }
}

function parse(_ctx, stream) {
  let la = TokenStream.peek(stream);
  let match = la.kind;
  if (typeof match === "object") {
    return;
  }
  if (match !== "LAngle") {
    return;
  }
  let startPos = la.position;
  TokenStream.next(stream);
  let textRef = "";
  let endPosRef = la.endPosition;
  let keep = true;
  while (keep) {
    let tok = TokenStream.peek(stream);
    let match$1 = tok.kind;
    let exit = 0;
    if (typeof match$1 !== "object") {
      switch (match$1) {
        case "RAngle" :
          endPosRef = tok.endPosition;
          TokenStream.next(stream);
          keep = false;
          break;
        case "Newline" :
        case "EOF" :
          keep = false;
          break;
        default:
          exit = 1;
      }
    } else {
      exit = 1;
    }
    if (exit === 1) {
      textRef = textRef + tok.text;
      TokenStream.next(stream);
    }
  };
  let text = textRef.trim();
  let id = Slugify.slugify(text === "" ? "link" : text);
  let node_location = {
    start: startPos,
    end_: endPosRef
  };
  let node = {
    location: node_location,
    id: id,
    text: text
  };
  return {
    TAG: "LinkNode",
    _0: node
  };
}

function make() {
  return V2ElementParser.make("Link", Priority.link, canParse, parse);
}

let priority = Priority.link;

export {
  priority,
  canParse,
  parse,
  make,
}
/* No side effect */
